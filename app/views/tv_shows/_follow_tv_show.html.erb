<%= form_for(current_user.tv_relationships.build(tv_show_id: $curr_show.id), remote: true) do |f| %>
  <div><%= f.hidden_field :tv_show_id %></div>
  <%= f.submit "Follow", class: "btn btn-large btn-primary" %>
<% end %>