<%= form_for(current_user.movie_trackers.build(movie_id: $curr_movie.id), remote: true) do |f| %>
  <div><%= f.hidden_field :movie_id %></div>
  <%= f.submit "Need to Watch", class: "btn btn-large btn-primary" %>
<% end %>