<%= form_for(current_user.movie_trackers.find_by_movie_id($curr_movie),
             html: { method: :delete }, remote: true) do |f| %>
  <%= button_tag( class: "btn btn-success") do %>
    <i class="icon-ok icon-white"></i>
  <% end %>
<% end %>